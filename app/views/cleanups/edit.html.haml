%ul
  - @cleanup.items.order(:category).each do |item|
    %li
      = button_to "#{item.category_name} +", plus_cleanup_item_path(item), { class: 'item' }
      = item.quantity
      = button_to "-", minus_cleanup_item_path(item), { class: 'item' }

= button_to 'Submit', submit_cleanup_path(@cleanup)
